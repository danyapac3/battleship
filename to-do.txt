features:
  ✓ Add ship rotation during placement (Space key and mouse wheel).
  ✓ Show a hover preview indicating where a ship will be placed.
  ✓ When placing ships make ready button inactive while there are unplaced ships remain.
  Add a popup prompting the player to pass the device to the opponent.
  Add a Randomize button to automatically populate the gameboard.
  Add a game mode where the player plays against a bot.
  (low priority) Highlight hover is red when ship can't be placed.

fixes:
  highlight stays visible at where ship was placed and still can be rotated. 

refactor:
  magic numbers in game-screen init function can be changed with gameboard BOARD_SIZE;
  orientation values for gameboard.placeShip have to be imported from gameboard file; 
  separate logic for rendering and updating boards;

general:
  Deploy the game to GitHub Pages.
  Add a project description to the README.